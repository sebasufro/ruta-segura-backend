# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=ruta_segura_db
PG_PORT=5432

# JWT
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRATION=7d

# App
PORT=3000

# Solo para desarrollo local sin Docker (apunta a localhost)
DATABASE_URL="postgresql://postgres:change_me@localhost:5432/ruta_segura_db?schema=public"
